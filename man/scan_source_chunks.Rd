% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blocklist.R
\name{scan_source_chunks}
\alias{scan_source_chunks}
\title{Extract chunk byte references for one array in one source file}
\usage{
scan_source_chunks(scan_path, source_array, ref_path, A, contiguous)
}
\arguments{
\item{scan_path}{Path opened to read the chunk index (the source's \code{access}).}

\item{source_array}{Array name within the file, e.g. \code{"/anom"}.}

\item{ref_path}{Durable URL written into the \code{path} column (the \code{public} URL).}

\item{A}{Array description from \code{\link[=parse_mosaic_vrt]{parse_mosaic_vrt()}} with at least \code{shape} and
\code{chunks} (C order); \code{chunks} must be the authoritative HDF5 storage chunk.}

\item{contiguous}{Logical; if \code{TRUE} the dataset is unchunked and a single
whole-array reference is emitted instead of iterating a chunk index.}
}
\value{
A \code{data.frame}, one row per chunk: \code{ndim} integer coordinate columns
(\code{c1}..\code{cN}), \code{offset} (byte address), \code{size} (byte length), \code{path}.
}
\description{
Opens \code{source_array} in \code{scan_path} read-only and walks its HDF5 chunk index
(\code{H5Dchunk_iter}) to recover, per chunk, the grid coordinate (C order) and
the byte address and length of the stored, compressed data. Coordinates are
local to the source; the caller shifts them to global position using the
mosaic \code{DestSlab} offset. A non-zero filter mask on any chunk aborts with an
error - it means the chunk does not share the array-level codec and cannot be
virtualised uniformly.
}
