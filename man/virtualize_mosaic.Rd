% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vrefs.R
\name{virtualize_mosaic}
\alias{virtualize_mosaic}
\title{Virtualise a GDAL mosaic into a kerchunk-Parquet reference store}
\usage{
virtualize_mosaic(vrt, root, sources, record_size = 100000L)
}
\arguments{
\item{vrt}{Path to a VRT written by \verb{gdal mdim mosaic} over \code{sources$access}.}

\item{root}{Output directory for the store.}

\item{sources}{A \code{data.frame} with columns \code{access} and \code{public}, typically
from \code{\link[=mosaic_sources]{mosaic_sources()}}. Joined to the VRT by \code{access} (the
\code{SourceFilename}), with a basename fallback.}

\item{record_size}{Integer chunk references per parquet shard (default 1e5).}
}
\value{
The store \code{root}, invisibly.
}
\description{
End-to-end driver. Parses a \verb{gdal mdim mosaic} VRT for the logical layout,
probes each array's codec and storage chunk shape once via rhdf5, scans every
source file for chunk byte references, composes them across files using the
VRT \code{DestSlab} offsets, inlines coordinate variables (values from the VRT, CF
attributes read from a source), and writes the store. The output is both a
portable reference store and the input to a VirtualiZarr -> Icechunk step.
}
\examples{
\dontrun{
src <- mosaic_sources(public = urls, access = nfs_paths)
system(sprintf("gdal mdim mosaic \%s mosaic.vrt", paste(src$access, collapse = " ")))
virtualize_mosaic("mosaic.vrt", "oisst.zarr", sources = src)
}
}
\seealso{
\code{\link[=mosaic_sources]{mosaic_sources()}}, \code{\link[=parse_mosaic_vrt]{parse_mosaic_vrt()}}, \code{\link[=write_kerchunk_parquet]{write_kerchunk_parquet()}}
}
