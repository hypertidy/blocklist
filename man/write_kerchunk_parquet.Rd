% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vrefs.R
\name{write_kerchunk_parquet}
\alias{write_kerchunk_parquet}
\title{Writes the fsspec \code{LazyReferenceMapper} layout (Zarr v2): a root \code{.zmetadata}
holding consolidated array/group metadata plus \code{record_size}, and a
\verb{<var>/refs.<N>.parq} shard per array. Each row is either a chunk reference
(\code{path}/\code{offset}/\code{size}) or inline data (\code{raw}); rows are placed at the
C-order flat chunk index and padded to \code{record_size}. The result is readable
by VirtualiZarr (\code{KerchunkParquetParser}), the GDAL Zarr driver, and any
fsspec reference filesystem.}
\usage{
write_kerchunk_parquet(
  root,
  vars_meta,
  ref_tables,
  inline = list(),
  record_size = 100000L,
  root_attrs = list()
)
}
\arguments{
\item{root}{Output directory for the store (created, overwriting any existing).}

\item{vars_meta}{Named list of per-variable metadata; each entry has \code{shape},
\code{chunks}, \code{dtype}, \code{compressor}, \code{filters}, \code{fill_value}, \code{dim_names},
\code{attrs}.}

\item{ref_tables}{Named list (matching \code{vars_meta}) of chunk-reference
\code{data.frame}s from \code{\link[=scan_source_chunks]{scan_source_chunks()}}; omit for variables given via
\code{inline}.}

\item{inline}{Named list of variables stored inline; each entry has \code{raw},
the bytes of a single whole-array chunk.}

\item{record_size}{Integer chunk references per parquet shard (default 1e5).}

\item{root_attrs}{Named list of group-level attributes for the root \code{.zattrs}.}
}
\value{
The store \code{root}, invisibly.
}
\description{
Writes the fsspec \code{LazyReferenceMapper} layout (Zarr v2): a root \code{.zmetadata}
holding consolidated array/group metadata plus \code{record_size}, and a
\verb{<var>/refs.<N>.parq} shard per array. Each row is either a chunk reference
(\code{path}/\code{offset}/\code{size}) or inline data (\code{raw}); rows are placed at the
C-order flat chunk index and padded to \code{record_size}. The result is readable
by VirtualiZarr (\code{KerchunkParquetParser}), the GDAL Zarr driver, and any
fsspec reference filesystem.
}
\seealso{
\code{\link[=virtualize_mosaic]{virtualize_mosaic()}}
}
