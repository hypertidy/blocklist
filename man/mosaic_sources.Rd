% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blocklist.R
\name{mosaic_sources}
\alias{mosaic_sources}
\title{Build a sources table for \code{\link[=virtualize_mosaic]{virtualize_mosaic()}}}
\usage{
mosaic_sources(public, access = public)
}
\arguments{
\item{public}{Character vector of durable URLs to record in the references
(e.g. \verb{https://} or \verb{s3://}), one per source file.}

\item{access}{Character vector, same order as \code{public}, of the paths actually
opened during the build (handed to \verb{gdal mdim mosaic} and opened by rhdf5).
Defaults to \code{public} for the all-remote case.}
}
\value{
A \code{data.frame} with columns \code{access} and \code{public}, one row per source.
}
\description{
Pairs each source file's \emph{access} path (what \verb{gdal mdim mosaic} is given and
what rhdf5 opens to scan) with its \emph{public} URL (what is written into the
reference store). Chunk byte offsets are identical in any byte-identical copy
of a file, so the two may differ freely: scan a cheap local or NFS path while
referencing a durable remote URL.
}
\examples{
\dontrun{
mosaic_sources(
  public = c("https://host/day01.nc", "https://host/day02.nc"),
  access = c("/nfs/day01.nc",        "/nfs/day02.nc"))
}
}
\seealso{
\code{\link[=virtualize_mosaic]{virtualize_mosaic()}}
}
