% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vrefs.R
\name{parse_mosaic_vrt}
\alias{parse_mosaic_vrt}
\title{Parse a GDAL multidimensional mosaic VRT}
\usage{
parse_mosaic_vrt(vrt)
}
\arguments{
\item{vrt}{Path to a \code{.vrt} written by \verb{gdal mdim mosaic}.}
}
\value{
A list with \code{dim_size} (named integer vector of dimension extents),
\code{arrays} (named list of data-variable descriptions, each with \code{dim_names},
\code{dtype}, \code{shape}, \code{chunks}, \code{scale}, \code{offset}, \code{nodata}, \code{unit}, \code{sources}),
and \code{coords} (named list with \code{dim}, \code{dtype}, \code{values}).
}
\description{
Reads the logical manifest produced by \verb{gdal mdim mosaic}: dimension sizes,
and for each data array its C-order dimension names, dtype, shape, GDAL block
size, CF \code{scale}/\code{offset}/\code{nodata}/\code{unit}, and the ordered list of sources
(each with \code{SourceFilename}, \code{SourceArray}, and \code{DestSlab} offset).
Coordinate arrays are returned with their composed values (from the VRT's
\code{InlineValues} or \code{RegularlySpacedValues}). No data or chunk bytes are read.
}
\seealso{
\code{\link[=virtualize_mosaic]{virtualize_mosaic()}}
}
